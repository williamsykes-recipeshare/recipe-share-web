import { styled } from '@mui/material';
import { MaterialDesignContent } from 'notistack';

const InfoSnackbarMaterialDesignContent = styled(MaterialDesignContent)(() => ({
}));

export default InfoSnackbarMaterialDesignContent;