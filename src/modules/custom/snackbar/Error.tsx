import { styled } from '@mui/material';
import { MaterialDesignContent } from 'notistack';

const ErrorSnackbarMaterialDesignContent = styled(MaterialDesignContent)(() => ({
}));

export default ErrorSnackbarMaterialDesignContent;